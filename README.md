# claudeSkills

1) installa claude cli:
curl -fsSL https://claude.ai/install.sh | bash
2) instalalzione mcp server per vederlo in vs code dentro l'estenzione:
claude mcp add --scope user playwright -- npx @playwright/mcp@latest